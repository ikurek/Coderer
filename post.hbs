{{!< default}}

{{! The comment above "< default" means - insert everything in this file into
    the {body} of the default.hbs template, which contains our header/footer. }}

<div id="content-main">

  <article class="{{post_class}}">

    {{! Everything inside the #post tags pulls data from the post }}
    {{#post}}

    {{! Everything below outputs content of the the post which has been published }}
    <h2 class="post-title"><a href="{{url}}">{{title}}</a></h2>
    <span class="post-meta"><time datetime="{{date format='YYYY-MM-DD'}}">{{date format="MMMM D, YYYY"}}</time> {{#if tags}} | Tags: {{tags}}{{/if}}</span>

    </br>
    {{#primary_author}}
      <span class="post-meta">Created with love by <a href="{{url}}" style="color: #4183c4">{{name}}</a> {{#if location}} | {{location}} {{/if}}</span>
    {{/primary_author}}

    <section class="post-content">
      {{content}}
    </section>

    {{/post}}

  </article>

</div>
